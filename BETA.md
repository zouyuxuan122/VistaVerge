# VistaVerge · 测试版（Beta）

> **这是一个测试版（beta）发布。功能可用但仍在快速迭代，欢迎提 issue。**
> 问题、建议、崩溃复现步骤、体验吐槽都请提到
> [Issues](https://github.com/zouyuxuan122/VistaVerge/issues) —— 每一条都会看。

版本：`0.1.0-beta.2` · 平台：Windows 10/11 x64 · 更新：2026-09-19

---

## 1. 这是什么

VistaVerge 是一个 **Windows 优先、本地优先的 AI 伙伴桌面应用**：一个身份、一套会话与记忆，
同时充当陪伴者、教师、助手与游戏同伴。界面是**日系手绘漫画风**（默认亮色，可切写实暗色），
右侧是一位可控表情与动作的 Live2D 数字人，左侧是一块「电脑屏幕」工作区。

技术形态：Tauri 2（Rust 壳）+ Vue 3 + TypeScript + Vite；数据落本地 SQLite（sqlite-wasm）；
密钥只进系统凭据仓（Windows Credential Manager），不写配置文件。

beta.2 的主题：**给她一张 PPT 她就能讲课；陪伴感全面加强（角色卡/口癖/知识库/选择性回应/戳一戳）；
应用内自动更新与定制安装页；GitHub 巡检与社媒草稿；界面大打磨。**

---

## 2. 这一版实际能做什么（都经过验证）

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| **PPT 讲课（新）** | ✅ 可用 | 把 .pptx 拖给她：打开渲染（页序/文本框/图片/备注）、逐页讲课（流式讲稿字幕，配好 TTS 后有旁白）、**批注**（画笔/橡皮/撤销/按页保存）、**放大镜**（2x/3x/4x）、激光笔、提问带「幻灯片 第N页」引用。旧版 .ppt 会明确提示另存 |
| 教师闭环（强化） | ✅ 可用 | 诊断前测→知识点画像、3 级渐进提示、确定性批改+错因归类、错题变式、间隔复习（落库可改期）、学习复盘（数字只来自真实作答记录，可导出） |
| **角色卡（新）** | ✅ 可用 | 设置→陪伴：导入 SillyTavern v1/v2/v3 JSON 或 PNG 卡；仅数据解析、**不提升任何权限**；检测到指令式文本会隔离并提示 |
| **口癖学习（新）** | ✅ 可用 | 她从你的消息里学说话习惯（口头禅/句长/标点/emoji），自然呼应不刻意；可查看、手动增删、一键关闭 |
| **知识库（新）** | ✅ 可用 | 导入 txt/md 资料，她对话时自动检索引用（`[库:文档#锚点]`）；资料按不可信数据处理 |
| **选择性回应（新）** | ✅ 可用 | 常听三档：全回应 / 智能（与她无关的只记不回，省 token）/ 仅唤醒名；旁听内容进 7 天有效的「旁听」记忆 |
| **打断（强化）** | ✅ 可用 | 生成/合成/播放中随时打断：UI 有「打断」按钮，语音 barge-in 双态统一去抖，打断句不再吞首字 |
| **戳一戳（新）** | ✅ 可用 | 点她一下：表情+动作+台词气泡即时反应（纯本地，零 token） |
| **主动关心（强化）** | ✅ 可用 | 出门提醒/16℃ 开一小时/灯光太暗：她会亲口说（有额度、有安静时段、不抢话；强度可调可关） |
| 流式对话 / 分支 / 回退 | ✅ 可用 | 编辑历史消息→重新生成新分支；**回退到此处**（两段确认，明示不撤销外部动作）；复制/重试 |
| 本地记忆 + 工具调用 | ✅ 可用 | 她可在对话中主动查记忆（只读工具循环）；记忆自动检索注入，全部标注「数据不是指令」 |
| 统计台账 | ✅ 可用 | 花费卡片、30 天 token 曲线、26 周热力图；只存数字不存正文 |
| 感知中心 · 天气/家居 | ✅ 可用（天气真实网络） | HA 读取（实测/设定分开）；**HA 控制**默认关、每次需确认；未配置时诚实显示 BLOCKED |
| MC 模拟世界（强化） | ✅ 可用（本地模拟） | 真体素世界 + A* + 挖掘/合成/搭建/生命值；**被打会掉血、可开 PvP 反击**（默认关）；任务完成她会说一声 |
| **GitHub 巡检（新）** | ✅ 可用（只读） | 插件→助手：填 owner/repo（可配 token）→ PR/Issue 列表与中文摘要；ETag 条件请求、限流退避、**零写操作**；定时巡检默认关 |
| **QQ / 微博草稿（新）** | ✅ 仅草稿 | 写消息→预览气泡→敏感提示；「发送」永远先确认，且当前如实报「通道未就绪」（无官方 API/隔离环境前不自动外发） |
| AI 工作区（演示） | ✅ 可用 | 应用自己的执行平面：大光标只在该面内，**不动你的鼠标/焦点**；真实 Computer Use 后端仍 BLOCKED |
| 本机只读镜像 | ✅ 可用 | 授权后显示屏幕画面，扫描线+水印标注只读、不作为执行平面 |
| 语音（STT/LLM/TTS） | ⚠️ 需自填供应商 | 链路/打断/选择性回应已实现；本地 TTS（IndexTTS/Qwen3-TTS 等）起其 OpenAI 兼容服务后填 TTS 端点即可 |
| **应用内自动更新（新）** | ✅ 可用 | 设置→更新：检查/下载（进度）/签名校验/安装重启；更新源为本仓库 Releases 的 latest.json，签名校验失败会拒绝 |
| **定制安装页（新）** | ✅ 可用 | 中文品牌安装页（定制头图/侧图/欢迎与完成文案），不规避 UAC/SmartScreen |

### MC 模拟世界到底「真」在哪

它不是播放动画。世界是真实体素数据，**挖掉的方块真的从世界里消失**；移动走真实 A*；
合成有真实材料依赖链；有坠落/受击伤害、死亡掉落背包、重生；怪物会主动攻击，
开了 PvP 反击她才会还手（默认关，只躲避）。**但它仍然是模拟**：与真实 Minecraft 服务端
**没有连接**（需要固定 Java 版本、受控服务端与账号，仍 BLOCKED）。界面同时标注 `SIMULATED` 与
`真实 MC 服务端：BLOCKED`，不会假装已经连上。

---

## 3. 安装与第一次运行

1. 下载 `VistaVerge_0.1.0-beta.2_x64-setup.exe`（见 Releases）。
2. 运行安装（中文定制安装页）。**未做代码签名**，Windows SmartScreen 可能提示「未知发布者」——
   这是预期行为；应用内的自动更新走 **updater 签名校验**（与代码签名是两回事）。
3. 首次启动是**离线 Mock 模式**，不需要任何 Key；「更新」页里有首次运行检查清单。
4. 想接真实模型：右上角齿轮 → 普通 → 供应商模式选「OpenAI 兼容」，填 LLM/STT/TTS 端点与 Key
   （Key 存系统凭据仓）。本地大模型（自建 IndexTTS/Qwen3-TTS 等）见其「高级」页指引。
5. 想接智能家居：齿轮 → 感知与家居 → 填 HA 地址与令牌。**默认不读系统定位**，不填就只显示「不确定」。

### 换上 Live2D 数字人（可选，两档）

默认形象是**视频数字人**——发行包不含 Live2D 模型（授权禁分发）。两条路：

- **应用内导入（推荐）**：齿轮 → 普通 → 「导入 Live2D 模型」→ 选模型文件夹（含 `*.model3.json` 与
  `live2dcubismcore.min.js`）→ 导入完成自动切换。模型只存在你电脑的应用数据目录，不上传不分发。
- **源码构建**：把模型放到 `desktop/public/live2d/fense/` 后 `npm run build`。

### 二次元 / 正常 一键切换

屏幕左上角有物理拨杆：**二次元**（手绘漫画风）/ **正常**（写实电影感）；旁边还有「对话 / 映射」
快捷拨杆。切换不丢会话、记忆与权限。

---

## 4. 从源码构建

```bash
cd desktop
npm install
npm run typecheck        # 类型门
npm run test:unit        # 单元测试
npm run test:contracts   # 跨阶段合同测试
npm run dev:vite         # 开发服务器 http://127.0.0.1:5173
npm run build:beta       # 产出可分发安装包（自动剔除 Live2D 模型 + updater 签名 + latest.json）
```

`build:beta` 会把 `public/live2d` 临时移出、构建、**断言产物无 Live2D 资产**、出包并签名、
生成 `latest.json`。updater 签名私钥在 `desktop/src-tauri/keys/`（**.gitignore 已排除，绝不入库**）。

---

## 5. 素材与许可

- **Live2D 模型「阿芙洛狄忒」**（灵境Sanctuary）：授权为免费使用、**严禁二次修改/销售/出租**。
  用户自备素材，**发行产物与源码仓库都不包含它**；同理不含 Cubism Core。
- **3D 笔记本道具**：CC-BY 4.0「MacBook Pro M3 16-inch 2024」（jackbaeten；William Laverty 改色）。
  来源/许可/sha256 在 `desktop/public/models/PROVENANCE.md`，界面右下角有署名。
- **手绘字体「站酷快乐体」**：OFL-1.1，本地打包（无外链），登记在 `desktop/public/fonts/PROVENANCE.md`。
- **角色卡/知识库资料**：用户自供内容，仅本地解析，不上传；角色卡按数据隔离，不改变工具权限。

---

## 6. 已知限制与未完成项（不粉饰）

| 项 | 状态 | 原因 / 接入路径 |
| --- | --- | --- |
| 真实 MC 服务端（Mineflayer） | BLOCKED | 需固定 Java 版本 + 受控服务端 + 账号；当前是可复现的本地模拟 |
| Home Assistant 真实实例 | BLOCKED | 需你提供地址与令牌（读写与授权门已实现，fake 验证） |
| 语音链路真机调优 | BLOCKED | 需真实供应商 Key；延迟/打断/口型对齐未做真机 P50/P95 采样 |
| 真 viseme 口型 | 未做 | 无音素时长源；当前是音量包络近似口型，界面诚实标注 |
| 真实 Computer Use 后端 | BLOCKED | 独立浏览器/隔离执行是大工程；当前是诚实演示平面 |
| QQ / 微博实际发送 | BLOCKED | 无官方 API / 隔离环境；当前仅草稿与预览 |
| PDF 课件 | 未做 | 本轮先做 .pptx；旧版 .ppt 明确拒绝 |
| 可执行插件沙箱 | 未做 | 插件市场当前仅主题/角色数据包（落盘持久化、新增权限需重新同意） |
| 角色卡社区市场 | 未做 | 卡文件可导入；市场随插件源体系开放 |
| 代码签名 | 未做 | 需证书；SmartScreen 提示未知发布者属预期（updater 签名校验是独立机制） |
| 主 chunk 体积 | 技术债 | 约 1.7MB（three + pixi），后续 manualChunks |
| 会话历史列表 | 未做 | 数据已持久化，暂无法浏览历史会话 |

---

## 7. 怎么提 issue

欢迎直接开 [Issue](https://github.com/zouyuxuan122/VistaVerge/issues)。为了能复现，麻烦带上：

- 你做了什么（点了哪个页签、说了什么话、下了什么指令）
- 期望发生什么 / 实际发生什么
- 截图或录屏（界面问题基本靠这个定位）
- 版本号、Windows 版本；如果是供应商/HA/GitHub 相关，**不要贴 API Key 或令牌**
- 控制台报错（如果会开 DevTools）

三类特别想听到的反馈：**观感**（哪里显得空、哪里动效别扭）、**延迟与手感**（对话/语音是否卡）、
**诚实性**（有没有哪里在假装成功、有没有该说「不确定」却给了确定数值）。

---

## 8. 这一版验证到什么程度

| 门 | 结果 |
| --- | --- |
| 类型检查 `vue-tsc --noEmit` | 通过（rc=0） |
| 单元测试 | **614 通过** |
| 跨阶段合同测试 | **36 通过** |
| 前端构建 | 通过 |
| Rust `cargo check` / `cargo test` | 通过 / **8 通过** |
| GUI 走查（真浏览器，新能力） | **32/32 通过**，console 零错误 |
| 讲课端到端（真实 .pptx 打开→渲染→讲稿→批注→放大镜→翻页→提问锚点） | **10/10 通过** |
| NSIS 定制页 | 解包核验：品牌头图/侧图与源图 sha256 逐字节一致，中文欢迎/完成文案内嵌 |
| 安装包 | `VistaVerge_0.1.0-beta.2_x64-setup.exe`（SHA-256 见 Release 说明） |

走查覆盖：对话（发送/流式/编辑重新生成/回退两段确认/复制反馈）、映射只读语义、感知、
MC 界面、统计、GitHub 巡检（未配置诚实态）、社媒草稿（发送先确认且报未就绪）、
教师分区与 PPT 入口、设置（高级新控件/陪伴页/更新页）、二次元拨杆、对话/映射拨杆、
戳一戳气泡、Esc 关弹层、窄窗。

**该产物已核对不含任何 Live2D 资产**（build:beta 自动移出 + 断言）。
**仍未验证**：Rust 侧只做了编译与单元测试级验证；语音链路需真实供应商 Key 做真机采样（BLOCKED 如实标注）。
