# VistaVerge · 测试版（Beta）

> **这是一个测试版（beta）发布。功能可用但仍在快速迭代，欢迎提 issue。**
> 问题、建议、崩溃复现步骤、体验吐槽都请提到
> [Issues](https://github.com/zouyuxuan122/VistaVerge/issues) —— 每一条都会看。

版本：`0.1.0-beta.1` · 平台：Windows 10/11 x64 · 更新：2026-09-19

---

## 1. 这是什么

VistaVerge 是一个 **Windows 优先、本地优先的 AI 伙伴桌面应用**：一个身份、一套会话与记忆，
同时充当陪伴者、教师、助手与游戏同伴。界面是**日系手绘漫画风**（默认亮色，可切写实暗色），
右侧是一位可控表情与动作的 Live2D 数字人，左侧是一块「电脑屏幕」工作区。

技术形态：Tauri 2（Rust 壳）+ Vue 3 + TypeScript + Vite；数据落本地 SQLite（sqlite-wasm）；
密钥只进系统凭据仓（Windows Credential Manager），不写配置文件。

---

## 2. 这一版实际能做什么（都经过真机验证）

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| 手绘漫画风界面 | ✅ 可用 | 纸纹+半调网点、油墨双描线、漫画对话框、速度线；一键切写实暗色，切换不丢会话 |
| 视频数字人 | ✅ **默认形象**（发行包） | 三段状态素材（待机/聆听/说话），不可表情控制；素材同样用户自备，不入库 |
| Live2D 数字人 | ✅ 导入模型后可用 | 上半身、面向你、表情/动作可控、音量近似口型；发行版不含模型（授权原因，见 §5），可在设置里导入 |
| 流式对话 / 分支 / 重试 | ✅ 可用 | 编辑历史消息生成新分支，原分支保留；支持 OpenAI 兼容供应商与离线 Mock |
| 本地记忆 | ✅ 可用 | SQLite + FTS5 检索、标签、来源、忘记；插件页签内可查看/搜索/改/删/导出 |
| 统计台账 | ✅ 可用 | 花费卡片、30 天 token 曲线、GitHub 风 26 周热力图；只存计量数字，不存正文 |
| 教师（学习） | ✅ 可用 | 导入资料 → 带行锚点提问 → 生成小测 → 确定性批改给依据 → 错题本与复习排程 |
| AI 工作区（操控电脑演示） | ✅ 可用 | 应用自己的执行平面：大光标只在该面内移动，**不动你的鼠标/焦点**；动作带 surfaceId/frameId 可审计 |
| 本机只读镜像（电脑映射） | ✅ 可用 | 授权后显示屏幕/窗口画面，标注为只读、不作为执行平面 |
| 感知中心 · 天气 | ✅ 可用（真实网络） | Open-Meteo 公共接口（无需 Key），带观测/过期语义；过期就写「不确定」，绝不编造 |
| 感知中心 · 智能家居 | ⚠️ 适配器就绪，未配置 | Home Assistant WebSocket 已实现（握手/订阅/读实体）；未填地址+令牌时明确显示 BLOCKED |
| 感知中心 · 室内温度 | ⚠️ 需 HA | 严格区分**实测**（current_temperature）与**设定**（temperature），实体不可用时不猜数值 |
| 主动提醒（出门场景） | ✅ 可用 | 说「我要出门了」→ 查新鲜天气 + 检索「出行」记忆 → 合并一条提醒；安静时段不打扰，有反证则取消 |
| MC 模拟世界 | ✅ 可用（**本地模拟**） | 真实体素世界 + 真实 A* 寻路 + 真实挖掘/合成/搭建/生命值；可「边玩边聊」下达指令 |
| 语音（STT/LLM/TTS） | ⚠️ 需自填供应商 | 链路与打断已实现；无 Key 时不可用，界面诚实标注 |

### MC 模拟世界到底「真」在哪

它不是播放动画。世界是真实体素数据，**挖掉的方块真的从世界里消失**（界面上有剩余数量可以自己核对）；
移动走真实 A*，走不过去就如实失败；合成有真实材料依赖链（原木→木板→工作台→斧/镐→石头/矿），
缺材料会告诉你还缺什么；有坠落/受击伤害、死亡掉落背包、2 秒后重生；背包满了采集会失败并提示先收纳。

**但它仍然是模拟**：与真实 Minecraft 服务端**没有连接**（那需要固定 Java 版本、受控服务端与账号，
属未完成的 BLOCKED 项）。界面同时标注 `SIMULATED` 与 `真实 MC 服务端：BLOCKED`，不会假装已经连上。

---

## 3. 安装与第一次运行

1. 下载 `VistaVerge_0.1.0-beta.1_x64-setup.exe`（见 Releases）。
2. 运行安装。**未做代码签名**，Windows SmartScreen 可能提示「未知发布者」——这是预期行为，
   不是绕过安全提示；不放心可只跑源码（见 §4）。
3. 首次启动是**离线 Mock 模式**，不需要任何 Key，可以先把界面和 MC 模拟玩一遍。
4. 想接真实模型：右上角齿轮 → 普通 → 供应商模式选「OpenAI 兼容」，
   填 LLM/STT/TTS 的 baseUrl、模型名与 API Key（Key 存进系统凭据仓，不落配置文件）。
5. 想接智能家居：齿轮 → **感知与家居** → 填 HA 地址与长期访问令牌 → 测试连接。
   同时可在这一页填经纬度（默认北京）；**默认不读系统定位**，不填就只显示「不确定」。

### 换上 Live2D 数字人（可选，两档）

默认形象是**视频数字人**——因为发行包不含 Live2D 模型（授权禁分发），不该让报错当第一印象。
想要可表情/动作控制的 Live2D，两条路：

- **应用内导入（推荐，装好就能用）**：齿轮 → 普通 → 「导入 Live2D 模型」→
  选一个模型文件夹（里面要有 `*.model3.json`）→ 导入完成自动切换到 Live2D。
  注意 Cubism Core（`live2dcubismcore.min.js`）授权同样禁分发，要和模型放同一文件夹一起导入；
  没有它应用会明确告诉你缺什么。从 Live2D 官网下载 Cubism SDK for Web 即可拿到。
  导入的模型只存在你电脑的应用数据目录（`live2d\imported\`），不上传、不分发。
- **源码构建（本地开发）**：把模型放到 `desktop/public/live2d/fense/` 后 `npm run build` ——
  构建时会检测模型存在与否并自动把默认形象定为 Live2D。

---

## 4. 从源码构建

```bash
# 前置：Node 20+、Rust 稳定版、Windows 上需 WebView2（Win11 自带）
cd desktop
npm install
npm run typecheck        # 类型门
npm run test:unit        # 单元测试
npm run test:contracts   # 跨阶段合同测试
npm run dev:vite         # 开发服务器 http://127.0.0.1:5173
npm run build:beta       # 产出可分发安装包（自动剔除 Live2D 模型）
```

`npm run build:beta` 会把 `public/live2d` 临时移出、构建到 `dist-beta`、**再移回**，
并在出包前断言产物里没有任何 Live2D 资产；确认通过才会调用 Tauri 打包。
产物在 `desktop/src-tauri/target/release/bundle/nsis/`。

`desktop/src-tauri/icons/` 里是**原创占位图标**（几何标记），只为让 clone 后可直接构建。
换品牌图标：`npx tauri icon 你的图.png`。

---

## 5. 素材与许可

- **Live2D 模型「阿芙洛狄忒」**（灵境Sanctuary）：授权为免费使用、**严禁二次修改/销售/出租**。
  本项目把它当**用户自备素材**：本地开发可用，**发行产物与源码仓库都不包含它**。
  同理不含 Cubism Core。这是本仓库不做「开箱即用数字人」的唯一原因，不是功能缺失。
- **3D 笔记本道具**：CC-BY 4.0「MacBook Pro M3 16-inch 2024」（jackbaeten；William Laverty 改色/拆分）。
  来源、许可与 sha256 登记在 `desktop/public/models/PROVENANCE.md`，界面右下角有署名。
  「MacBook」为 Apple 商标，此处仅作描述性指称，与 Apple 无关联。
- **根许可证**：本仓库尚未发布根 LICENSE 文件，这**不改变**已存在第三方代码各自的许可。
  未获授权的素材不纳入默认分发。

---

## 6. 已知限制与未完成项（不粉饰）

| 项 | 状态 | 原因 / 接入路径 |
| --- | --- | --- |
| 真实 MC 服务端（Mineflayer） | BLOCKED | 需固定 Java 版本 + 受控服务端 + 账号；当前是可复现的本地模拟 |
| Home Assistant 真实实例 | BLOCKED | 需你提供地址与令牌（适配器已就绪，测试覆盖握手/超时/读数语义） |
| 语音链路真机调优 | BLOCKED | 需真实供应商 Key；延迟/打断/口型对齐尚未做真机 P50/P95 采样 |
| 真实 VRM 资产 | BLOCKED | 需授权素材；三维形象保留为占位与 VRM 接入路径 |
| 代码签名 / 自动更新 | 未做 | 需证书；因此 SmartScreen 会提示未知发布者 |
| 主 chunk 体积 | 技术债 | 约 1.5MB（three + pixi），后续 manualChunks 分包；不影响功能 |
| 会话历史列表 | 未做 | 数据已持久化，但 UI 每次启动新建会话，暂无法浏览历史会话 |
| QQ / 微博 | 仅草稿 | 需合法 API 或隔离环境 + 授权 |

---

## 7. 怎么提 issue

欢迎直接开 [Issue](https://github.com/zouyuxuan122/VistaVerge/issues)。为了能复现，麻烦带上：

- 你做了什么（点了哪个页签、说了什么话、下了什么指令）
- 期望发生什么 / 实际发生什么
- 截图或录屏（界面问题基本靠这个定位）
- 版本号、Windows 版本；如果是供应商/HA 相关，**不要贴 API Key 或令牌**
- 控制台报错（如果会开 DevTools）

三类特别想听到的反馈：**观感**（哪里显得空、哪里动效别扭）、**延迟与手感**（对话/语音是否卡）、
**诚实性**（有没有哪里在假装成功、有没有该说「不确定」却给了确定数值）。

---

## 8. 这一版验证到什么程度

| 门 | 结果 |
| --- | --- |
| 类型检查 `vue-tsc --noEmit` | 通过（rc=0） |
| 单元测试 | **386 通过** |
| 跨阶段合同测试 | **36 通过** |
| 前端构建 | 通过 |
| Rust `cargo check` / `cargo test --lib` | 通过 / **8 通过** |
| GUI 走查（真浏览器，Playwright，本地构建含模型） | **77/77 通过**，console 零错误 |
| **桌面端到端（真实 Tauri 窗口，CDP 驱动）** | **13/13 通过**：导入 → 自动切 Live2D → 模型渲染 |
| 生产包冒烟（安装包内嵌的那份前端，无模型） | **17/17 通过**，console 零错误 |
| 安装包 | `VistaVerge_0.1.0-beta.1_x64-setup.exe`，6.60 MB |

安装包 SHA-256：`d4577b4dd81fab201c187095a063a894380dee7356d2a07d03ef7b60d0aa7aa1`（6918480 字节）

该产物已核对**不含任何 Live2D 资产**（模型与 Cubism Core 均未打入；`vistaverge.exe` ≈18.5 MB，
对照带模型的构建为 48.1 MB）。校验方式：在 `vistaverge.exe` 内检索 `fense.moc3` / `MOC3` / `live2dcubismcore`
均无命中，同时 `sql-wasm.wasm`、`macbook-source.glb` 有命中（证明检索方法有效）。

走查覆盖：主题与布局、对话流式与分支、只读镜像、工作区演示（含光标不越界）、
感知中心（真实天气网络请求、温度与 HA 的 BLOCKED 诚实态）、MC 模拟（真的挖到 6 个原木、
世界存量从 227 降到 221、受击掉血、跟随真的走路、无法识别的指令明确说「不猜」）、
教师全链路、插件市场与本地记忆、统计、设置、窄窗适配。

**已在真实桌面窗口验证**（启动打包好的 EXE，用 DevTools 协议驱动应用自己的 WebView，不注入宿主输入）：
「按路径导入模型 → 自动切到 Live2D → 模型经 vvmodel 协议加载渲染」13/13 通过，console 零错误。
另外确认了这一版修掉的 4 个**只在打包版暴露**的缺陷（dev 服务器不注入 CSP，所以此前一直没发现）：
Live2D 因 CSP 缺 `unsafe-eval` 起不来、笔记本贴图因 `connect-src` 缺 `blob:` 加载失败、
asset 协议的路径编码让贴图相对解析跑到 scope 外、导入落盘确认用了只读句柄导致 os error 5。

**仍未验证**：
前端是以真浏览器加载**同一份生产产物**验证的；Rust 侧只做了编译与单元测试级验证。
