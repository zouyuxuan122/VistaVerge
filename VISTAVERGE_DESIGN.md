# VistaVerge 总体计划书

版本：v1.1 / 2026-09-18 / **待负责人审批**。

本版吸收原始规划、真实模型与底部椭圆导航要求，以及隔离电脑操作、五感、本地标签记忆和真实体验调优补充。文档落盘不代表功能已经实现，也不代表允许自动执行全部控制包。

## 1. 产品目标与优先级

VistaVerge 是 Windows 优先的本地优先智能伙伴。陪伴、教师、助手、游戏同伴共享一个身份、会话、记忆、任务与权限体系。首要体验不是按钮数量，而是：能顺畅说话、能被打断、能记住相关事实、能在合适的时候提醒、能可靠完成授权任务。

优先级：

- **P0 基础与主体验：** 可维护工程、权限与数据、左屏右角色和底部悬浮导航、真正 VRM 模型、低延迟实时语音、统一取消。
- **P1 关键产品闭环：** 本地混合检索与情境感知、教师和陪伴、插件 SDK/仓库源市场、隔离 Computer Use、Minecraft。
- **P2 产品化：** Live2D 完整适配、家居与项目连接器、定制安装与签名更新、社区开发体验。
- **研究线：** 多图自动生成人物、自动绑定、更多游戏及跨设备。与稳定产品独立开关、独立验收，不能用“未来接口”宣称实现。

P1 不是可随意省略的愿望清单；它们是本项目核心目标，但在基础链路稳定后分别实现。

## 2. 不可突破的约束

### 2.1 功能真实性

现有角色是三段视频，不是骨骼模型。最终默认形象必须是有合法来源的实际 VRM；导入与市场发布分别检查对应版本 meta 和授权中的使用者、商业、修改与再分发限制。Live2D 用真实参数与动作组驱动，Cubism SDK 发布许可和模型再分发授权是独立前置门，未通过则对应发行 BLOCKED，不阻塞手绘 VRM。音量缩放整幅画面不算口型同步。漂亮静态 UI、不调用工具的演示、不带实际游戏连接的按钮不能算功能完成。

### 2.2 不干扰宿主电脑

AI 大光标**仅绘制于 VistaVerge 映射视口内**，不是桌面顶层光标窗口。默认执行平面为应用拥有的独立浏览器或隔离虚拟机。宿主 `SetCursorPos/SendInput/SetForegroundWindow` 不得成为隐藏降级路径。

本机屏幕“只读镜像”和“AI 独立工作区”分开命名与权限。映射不创造第二套输入环境。Windows 普通虚拟桌面、隐藏窗口、UI Automation 不等于输入隔离。首启探测 Windows 版本与隔离能力，Home 不假定具备 Hyper-V/Sandbox；缺隔离时仅保留 API/独立浏览器/草稿，不偷偷接管。独立浏览器只提供受控输入平面，不自动隔离文件、网络或凭据，这些仍需权限控制。原生 QQ 等可能需要在隔离环境安装和单独登录，不能假设可操作现有宿主登录而无干扰。

### 2.3 权限与隐私

可信核心管理凭据、权限、审计、资源限制、安装完整性与更新验证，插件不得替换或绕过。传感器、麦克风、天气位置、屏幕共享、私有仓库等均由用户授权。持续陪伴不等于无条件上传持续音频或记录一切。

角色卡/网页/插件/记忆/屏幕OCR/游戏聊天/连接器返回内容是数据，不是高优先级指令。涉及发送、购买、删除、账号变更、代码提交与家居控制的副作用需要对应授权。撤回聊天分支不等于撤销已执行的外部动作。

### 2.4 真实感与关系边界

“真实女友体验”落实为自然交流、长期连续性、共同活动和可控主动关心。不冒充真人、不以排他性或内疚话术诱导依赖；涉及未成年人时默认禁用恋爱模式。关心室温不是医学判断。

### 2.5 验收与维护

每个功能必须经历：合同测试 → 组件集成 → 真实端到端 → 使用场景调优 → 回归。外部依赖缺失标记 `BLOCKED`；模拟不能提升为真实验收 PASS。性能数据记录样本、硬件、模型、供应商、网络、冷暖启动、失败数与分位数。

## 3. 现状与重构决策

选择“保留 Tauri 2、重构应用层”。旧原生 JS 全局状态、批式语音、视频形象、无持久化/插件安全/测试基础不适宜继续堆叠。完全换成 AIRI/Electron 是备选而非默认：先用真实性能原型判断 Tauri 是否存在阻断。

可复用：音频采集与设备选择思路、供应商配置、上游 Realtime 协议、代际取消、聊天/工具结构。旧 `s2s/` 保留为来源可追踪参考，不把整套研究 Python 环境变成普通用户的强制依赖。

迁移采取旁路新入口与分阶段切换，旧用户设置和会话迁移要备份。移除明文 localStorage 密钥前先成功存入安全凭据仓，并验证读取，再清理旧副本；禁止丢失配置或日志泄密。

## 4. 目标架构

- Tauri 2 稳定版/Rust：原生权限、凭据、进程、采集与更新。
- Vue 3 + TypeScript + Vite + Pinia：组件化 UI、路由和展示状态。
- Three.js + three-vrm；Live2D 作为独立可选适配器；Blender 源场景导出 GLB。
- 受管理 Node sidecar：会话编排、任务与第一方插件；第三方可执行代码不与其共享无约束权限。
- SQLite：唯一记忆/会话写入服务；FTS5 + 可替换向量后端；本地 embedding 按需安装和加载。
- Python/ONNX/其他模型服务按需进程化，不默认常驻多个模型框架。

事件有 `sessionId/turnId/generation/traceId`，工具有 `taskId/actionId`，观测有 `observedAt/expiresAt/source`。实时音频用有界二进制通道，UI 状态不承载逐帧 PCM。取消后旧输出不得进入新回合。

“万物皆插件”涵盖模型、语音、角色、主题、领域工具、策略和面板；不涵盖允许任意替换授权裁决的安全漏洞。详见 [架构合同](docs/architecture/SYSTEM_CONTRACTS.md)。

## 5. 主界面与模型

左约 60% 是电脑屏幕工作区：对话 / 本机只读镜像 / AI 工作区 / 当前任务。右约 40% 为角色坐在精致电脑前的场景。底部悬浮椭圆胶囊导航：对话、学习、任务、插件、设置。导航不遮输入框或人物手部。

左上角写实/手绘切换改变全套视觉语言、角色渲染和场景风格，但不丢会话或权限。默认 VRM 优先：眨眼、视线、表情、讲话、倾听、打字、鼠标动作分层；手部 IK 与桌面接触必须美术验收。VRM 格式本身不能保证照片级写实。

角色前电脑包含合理尺度、独立键帽、倒角、线材、材质、接触阴影。左侧功能文字仍正常 DOM 渲染，不为了三维透视牺牲阅读。DeepSeek 金鱼娘在素材/品牌使用许可满足后作为可选社区角色包导入，不作默认品牌形象、不暗示背书，不假称已有授权。

## 6. 语音与双向交流

渐进 ASR → 回合/对象判断 → 流式 LLM → 稳定短句 → TTS → 有界音频播放 → 播放时钟驱动口型。供应商不支持流式就显示分句/批式能力，不伪装。用户可在生成、工具等待、合成和播放时打断；外放须验证回声消除，AEC 不稳则明确降级为半双工/按键说话。常听默认关闭，开启后本地 VAD/唤醒判断、可见拾音状态和一键静音；没有本地识别时不声称能免费理解全部环境语义。暖态首音目标 P50≤1.5s、P95≤3s，端到端打断目标 P95≤350ms，具体测量条件以验收矩阵为准，均非已实现指标。

AI 主动插话由确定事件、交流间隙和用户偏好仲裁，默认保守。快反应依赖本地控制与热路径，不是启动更多 LLM 子智能体。Qwen3-TTS、FireRedTTS 系列、IndexTTS 通过能力协商接入；具体版本、权重许可和硬件实测决定默认，不凭名称强绑定。

TTS 的发音、韵律、长句稳定性和首音；ASR 的中文错误率、噪声、断句、短词和说话对象；MC 的实际成功率均要做体验调优，不止“接口返回 200”。详见 [语音设计](docs/product/VOICE.md)。

## 7. 五感、记忆与主动提醒

“五感”是感知系统的产品表达，不假称硬件能闻味或尝味：

1. 听觉：经同意的语音及对话意图。
2. 视觉：获授权的画面、图像和游戏状态。
3. 环境：温湿度、照度、设备状态和天气。
4. 时间/活动：日程、当前任务、出门意图、安静时间。
5. 关系/上下文：用户偏好、此前承诺、携带物品、近期交流。

观测事实、推测和持久记忆分开。“我要出去了，再见”触发出门场景：查新鲜天气、检索今天出行相关记忆，合并为一条简短提醒。能依据近期对话说“今天要带的资料别忘了”，不能凭空断言“你没带钥匙”。用户明确告知或有可信观测支持的已带伞/不出门等反证取消提醒，不凭空推断携带物品。天气过期或无定位时说明不确定，不胡编实时天气。

记忆以本地数据库为准，支持手工/自动标签、关键词+语义混合检索、可选重排、来源引用、否定更正、有效期、导出和忘记。模型可主动 `memory.search`，不是把全部记忆塞进提示词。子智能体按权限、预算、超时和取消代际并行检索，唯一写入服务合并提议。

完整细节见 [记忆与感知](docs/architecture/MEMORY_PERCEPTION.md)。

## 8. 教师、陪伴与领域插件

教师闭环：导入资料 → 诊断 → 多种讲解 → 提示/练习 → 批改与证据 → 错因 → 变式与间隔复习 → 学习复盘。口语评分只有实际评测器才能提供；代码题运行需隔离。陪伴支持角色卡、可编辑长期记忆、共同活动与可控主动交流。

Computer Use：API 优先、独立浏览器优先、原生应用用真实隔离环境；单执行租约、急停与不可逆动作确认。MC：Java 固定版本和受控服务器，Mineflayer 本地反应+行为树/技能+LLM 高层规划，边玩边聊；不逐帧调用 LLM、不规避反作弊。

家居优先 Home Assistant；16℃一小时需要区分设定与实际温度、占用、断线和安静时间。GitHub PR/Issue 巡检默认只读，定时工作必须由用户配置。QQ/微博先草稿与预览，不默认自动外发。

## 9. 插件、安装与更新

插件清单包含 SDK 兼容、权限、依赖、许可证、设置 schema、校验与发行者。第三方默认无文件/网络/凭据访问，经隔离宿主和能力化 IPC 授权；独立进程本身不是完整沙箱，隔离未验证的原生插件须标为受信任代码并默认禁用。仓库源支持 GitHub/HTTPS；签名和哈希职责不同，权限增加需重新同意。安装事务校验路径穿越、解压体积、依赖循环；插件/角色/模型与应用独立更新。

定制 NSIS 页面+首次运行向导，不规避系统安全提示。按需下载大模型，显示体积/硬件与许可；更新显示日志、权限差异、重启安排。Tauri updater 签名、密钥轮换、撤销、数据库迁移备份和恢复必须预先设计，不把二进制回滚当成数据回滚。

## 10. 交付与状态

当前只交付：私有仓库创建、本地 Git 初始化、计划书、需求矩阵、研究依据、首包任务草案及验证结果。没有得到“执行控制包 X”前不动产品代码。

阶段顺序、研究闸门、约束和证据见 [交付路线](docs/roadmap/DELIVERY_PHASES.md)。所有文档遵循 [工程规范](ENGINEERING_SPEC.md) 和 [协作规则](AGENTS.md)。产品指标见 [验收矩阵](docs/quality/ACCEPTANCE_MATRIX.md)，指标是目标而非已完成声明。
