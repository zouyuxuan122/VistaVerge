# VistaVerge 工程规范

状态：设计基础 / 2026-09-18。权威上层：[最高设计](VISTAVERGE_DESIGN.md)。

## 1. 设计与变更顺序

用户需求 → 最高设计 → 子系统合同 → GAP_AUDIT → 控制包审批 → 执行 → 独立验证 → 授权提交。编写控制包与执行不混在同一流水线；缺验门或依赖没定的控制包退回制作，不边写业务代码边补要求。

下层文档不得放宽“宿主输入隔离、权限、数据可忘记、真实验收”约束。重要变更以 ADR 记录：上下文、选项、结论、成本、恢复路径。未测性能不可写成技术事实。

## 2. 目录、合同与依赖

保留 `desktop/` 工程位置；逐步新增 `desktop/src/app/`、`features/`、`avatar/`，`packages/contracts/`、`plugin-sdk/`，`services/runtime/`、`plugins/`、`tests/`。不要为了目录改名移动全部参考代码。

公开合同需版本、运行时校验、取消、错误码、超时、权限和兼容范围。依赖锁版本；提取上游代码记录仓库、提交、文件、许可证及修改。默认一个语音编排主路径，不同时部署多套重叠框架。凭据不经日志、快照、测试报告或配置导出。

## 3. 代码与测试

功能按红测→最小实现→覆盖边界→集成推进。正常路径、取消/重试、断网、空值、权限拒绝、损坏数据必须可观察。

FOUNDATION-01 建立新增机器门：`npm --prefix desktop run typecheck`、`npm --prefix desktop run test:unit`、`npm --prefix desktop run test:contracts`，并复跑已存在的 `npm --prefix desktop run build:vite`。**新增三个script目前不存在，不能现在声称通过；现有build也未在本轮运行。** Rust `cargo test --manifest-path desktop/src-tauri/Cargo.toml` 及原生权限/IPC断言归后续 SECURITY-DATA-01 / RUNTIME-01，不属于本包已建立的门。

UI 测试覆盖浏览器组件和真实 Tauri 壳，截图不可替代功能断言。特权 Windows 测试、实际模型、MC 服务器与真实账号分开标识，缺失标 BLOCKED，不以 skip 冒充 PASS。

## 4. 可观测性与调优

追踪按 `traceId/turnId/generation/taskId/actionId` 关联。默认只记事件与耗时，内容日志 opt-in 并脱敏。本地调优报告含样本来源、硬件、版本、模型/权重 hash、网络、失败率、P50/P95/P99、冷暖态；记录质量与 Token/CPU/GPU，禁止单纯压延迟损害正确性。

每批交付保留命令、退出码和证据路径。外部命令有 timeout，失败不吞；敏感原始数据留本地私有证据目录，入库仅放脱敏报告。

## 5. 持久化、迁移与故障

SQLite 由单写服务负责，迁移有版本、事务和回归 fixture；WAL 运行时不裸复制单个主文件作一致性备份，使用 Backup API 或合适的 VACUUM INTO 流程。索引是派生数据，可重建，不能取代记忆原始来源。

删除覆盖全文索引、向量、缓存、摘要、任务临时上下文和恢复流程。对备份明确保留期与删除规则；加密与锁定状态显式展示，不把普通 SQLite 说成加密。系统凭据用受限句柄，避免任意插件读取。

## 6. Git 与协作

本轮仅创建私有仓库和初始化本地 Git；HEAD 指向 planning/vistaverge-foundation，尚无首次提交（unborn），**不自动 commit/push**。用户明确授权提交/推送后才操作。上游原有许可证保留，项目许可证需负责人确定；创建仓库不等于授权公开源代码或素材。

首次入库使用 allowlist 并审阅暂存 diff；禁止不检查就 `git add .`。默认排除 `node_modules/target/dist`、旧下载、权重、声音、人像、真实转写、密钥与运行数据。旧素材排除导致无法干净构建时，必须在引入源码前补合法资源包/占位与获取说明，不能把不可复现源码快照宣称可运行。

唯一 Git 写入者为前台。任务分支工作；未经明确授权不合并默认分支。一个任务一个可回滚提交，或遵守已批准的明确批次。push 后核对工作分支与对应远程 SHA；不为了“三 SHA 一致”擅自合并 main。

## 7. 审查与完成定义

子智能体提供命令/日志/产物，不写 PASS。前台独立复跑关键验门、核对文件域、检查需求一致性；独立审阅者发现重要问题必须处理。只有实际实现并验收才能写 VERIFIED。

文档结构校验通过只能说明文档有效；不证明架构性能、真实 UI、美术或业务质量。控制包审批与软件 PASS 是两件事。

## 8. 发布与长期维护

语义化版本、稳定/预览渠道、SDK 兼容矩阵、依赖更新节奏、迁移测试、旧配置兼容、错误诊断导出、安全报告入口、许可证/SBOM、签名/密钥轮换和回滚演练纳入发布合同。

提供用户数据导出、卸载保留选择、插件停用恢复、崩溃重启后任务恢复/阻止重复副作用。默认不收集遥测，不自动花费、部署外部服务或公开发布。
