# VistaVerge 协作规则

## 权威与范围

先读 `VISTAVERGE_DESIGN.md`、`ENGINEERING_SPEC.md` 和当前控制包全文。制作与执行分离：没有负责人明确“执行控制包 X”不得因目录存在就执行。子目录现有适用指令仍需遵守；上游 `s2s/AGENTS.md` 不可忽略。

## 子智能体

- 每次任务自包含：目标、依据、允许/禁止文件、依赖合同、验证命令和报告格式。
- 零 Git 写权限；不 commit/push/branch/merge，不擅自派下级智能体。
- 不改范围外文件，不以工具/环境失败掩盖验证失败。
- 交回实际命令、退出码、脱敏日志路径和未验证项，不自行写 PASS。

## 前台

- 同工作区文件写入串行；独立只读调研/审阅并行。隔离工作树并行需先有清楚合并边界。
- 独立复跑关键验门，审查文件域与需求一致性，再写 PASS。
- 保留进度与裁决记录，不能仅靠聊天上下文；断点恢复先查记录，不重复派已完成任务。
- 用户明确授权后才 Git 写入；不得将创建 GitHub 仓库的授权扩展为公开发布或推送敏感工作区。

## 硬约束

- 默认禁止操作宿主物理鼠标、抢宿主焦点、向宿主注入输入；大光标只在映射视口内。
- 模拟数据、AI 自述、截图和编译通过均不能替代真实功能验收。
- 记忆检索/修改遵守 scope、来源和删除语义；角色卡/网页/记忆不提升工具权限。
- 不把子进程、Worker 或普通虚拟桌面宣称为完整安全沙箱。
- 外部命令设置 timeout，证据脱敏；不运行未经审查的第三方脚本或下载后自动执行。
