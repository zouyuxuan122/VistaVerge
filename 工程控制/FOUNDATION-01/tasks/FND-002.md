# FND-002 修复请求信号类型并验证取消

状态：PASS / 2026-09-18，见 ../ACCEPTANCE.md。依赖FND-001。

## 目标与依据

ENGINEERING_SPEC.md §3机器门、SYSTEM_CONTRACTS.md §4取消；已复现代码位置 `desktop/src/engine.ts:323–444`。

## 文件域

允许：`desktop/src/engine.ts`、`desktop/tests/unit/engine-abort.test.ts`。
禁止：package/lock/config、UI、权限/凭据、上游代码、流式重构。若测试运行器本身需要改动，应退回FND-001修复并复验，不越界。

## 步骤与约束

1. 添加红测：mock `@tauri-apps/plugin-http` 的fetch；`mic-capture.js?url` 优先用Vite原有资源解析，只有测试解析失败才mock为固定测试URL。用最小WebAudio/navigator桩驱动引擎start/stop和实际worklet onmessage，不访问真实麦克风或网络。
2. 驱动可识别的一次回合，fake STT返回文本、LLM返回文本、TTS返回已知PCM，检查三个fetch options.signal都是当前回合的同一AbortSignal，而非undefined/AbortController。
3. 在STT、LLM、TTS各自pending时调用stop：信号aborted=true，没有后继请求或迟到reply；重复stop不抛错。provider忽略取消仍resolve时，消费侧在await后检查停止/取消，避免迟到结果复活。
4. `_stt/_llm/_tts/_play`参数统一接收AbortSignal；fetch使用该signal。STT/LLM/TTS每次await之后、空文本分支状态改写和user-turn/reply派发之前、catch错误派发/状态改写之前均先检查本回合signal.aborted并退出；播放同样检查。补测stop后STT空文本/非空文本resolve、LLM/TTS迟到成功或普通错误reject，均无状态从idle复活、无迟到事件/后续请求。控制器仍仅由回合拥有，不把类型强转成any压错。
5. multipart返回按精确bytes转换为可接受的BodyInit（例如新分配长度正确的ArrayBuffer），保留字段与文件二进制；禁止把整个底层池buffer连偏移之外一起发送。测试从fetch捕获bytes确认WAV头和payload与实际输入一致。
6. 播放测试用假的AudioBufferSource：signal取消后stop一次且Promise正常结束；不能仅因为类型对了就认为真实取消可用。
7. 本任务仅修复信号/停止语义与body；完整流式、AEC、回合generation和barge-in体验属于REALTIME，不扩大重构范围。

## 验门

- `npm --prefix desktop run test:unit -- tests/unit/engine-abort.test.ts`：rc=0，STT/LLM/TTS逐阶段信号与取消、迟到结果、重复stop、body/playback断言通过。
- `npm --prefix desktop run test:unit`：rc=0，无回归，无skip。
- `npm --prefix desktop run typecheck`：rc=0，原6错误消除；strict不关闭、无ts-ignore绕过。
- `npm --prefix desktop run build:vite`：现有本地合法测试资源可用时rc=0；资源缺失标BLOCKED，不擅自添加未授权素材。通过构建仍不是新产品体验验收。

## 证据与回滚

交回红测/绿测、参数与字节断言、退出码与日志。前台独立复跑测试/typecheck；核对只改允许文件。不宣称此包已解决麦克风回声或所有旧引擎竞态。回滚为恢复本任务两文件改动，不影响FND-001。
