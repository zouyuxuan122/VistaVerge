# EXP-007 集成、GUI 验收、Tauri 构建与阻塞清单

## 目标
端到端联调与真实浏览器验证；产出桌面构建；形成外部阻塞清单。这是收口任务，允许修改前序任何文件修集成缺陷（一处一测）。

## 步骤
1. 全量联调：vite dev 启动，按 GUI 清单走真实浏览器（Playwright/webapp-testing），逐项截图+console零错误。
2. GUI 清单（≥12项）：首屏布局60/40与胶囊；主题往返；mock对话流式→复制→编辑分支→重试；语音按钮状态（无麦克风时明确提示）；镜像授权态；演示工作区虚拟光标只在面内；任务页；教师闭环；记忆增删查；市场用本地fixture源安装数据包；设置普通/高级；窄窗1280×720无重叠。
3. 桌面壳：`cargo check` rc=0；`npm --prefix desktop run build`（tauri build）产出 NSIS 或明确 BLOCKED（缺Rust/签名/资源则附证据）。keyring 命令集成冒烟（若可自动则测，否则标手动）。
4. 修复发现的问题（每修一项跑相关门）。
5. 写 SUMMARY：差距闭合表、外部 BLOCKED 清单（真实供应商密钥/真VRM/Live2D/MC/QQ/HA/签名）及各自接入路径、性能首测（启动耗时/构建体积）。

## 验门
- npm 全门 rc=0；GUI 清单全过且截图存档 `.vistaverge/evidence/EXP-01/gui/`；console 无 error。
- cargo check rc=0；tauri build rc=0 或 BLOCKED+复现命令+原因。
- ACCEPTANCE.md 三层记录完整；无 skip 充 PASS。
