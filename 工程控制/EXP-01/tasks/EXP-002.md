# EXP-002 数据层：SQLite、记忆、会话分支、凭据

## 目标
实现 MEMORY_PERCEPTION.md §2–5 首版与聊天持久化；密钥迁入 OS 凭据仓。

## 文件域
desktop/src/data/**、desktop/src/platform/**、desktop/src-tauri/src/lib.rs、src-tauri/Cargo.toml、Cargo.lock、desktop/tests/unit/data-*.test.ts。
禁止：services/ui/scene、其他任务文件。

## 接口（后续任务依赖，不得擅自改）
```ts
// data/db.ts
initDb(persist?: {read():Promise<Uint8Array|null>, write(b:Uint8Array):Promise<void>}): Promise<Db>
// data/memory.ts
proposeWrite({kind,text,tags?,scope?,ttlSeconds?,source?}): {id, created}|{duplicate:true,id}
searchMemory({query?,tags?,tagMode?:"any"|"all",topK?}): MemoryHit[] // {id,text,tags,kind,createdAt,score,stale}
correctMemory(id, newText): void   // 旧记 superseded，新记 supersedes 链
forgetMemory(id): void             // 主表+FTS+tombstone；rebuild 后不复活
exportMemory(): MemoryRecord[]
// data/conversations.ts
createConversation(), addMessage({conversationId,branchId,role,text,parentId}), editMessage(id,newText)→新分支, retryFrom(id), listBranches(convId), listMessages(branchId)
// platform/credentials.ts
setSecret(key,value)/getSecret(key)/deleteSecret(key) // Tauri keyring 命令；浏览器 dev 降级 sessionStorage 并标 dev-only
// platform/persistence.ts — Tauri fs 写 db 文件；浏览器 dev 降级 IndexedDB
```

## 步骤
1. sql.js 初始化（Node 测试与 webview 双 locateFile）；schema_version 迁移（幂等）。
2. 表：memories/memory_tags/sources/tombstones/index_meta/conversations/messages/branches/tasks（task 表仅建结构，EXP-006 用）；FTS5 trigram external-content。
3. search：scope 过滤先行；FTS MATCH + 短查询（<3字符）LIKE 回退；tag any/all；stale 标记不过滤仅降权。
4. forget：事务删主表+FTS delete 命令（旧值精确）+tombstone；rebuild 用触发器守护测试。
5. conversations：editMessage 创建新 branch（copy 语义=剪贴板在 UI 层）。
6. Rust：keyring crate，tauri commands set/get/delete_secret，service="VistaVerge"。
7. TDD：先测试后实现。

## 验门
- 全部门 rc=0；data 测试≥10用例：中文长短查询、短词回退、tag any/all、scope隔离、forget后MATCH与rebuild不复活、correct保留历史、迁移幂等、编辑出分支、凭据接口在Node侧有可测抽象（Rust命令不强制单测，标集成验证于EXP-007）。
- typecheck 覆盖 tests。
